# Pinned to the exact versions used to produce every number in the manuscript.
# Python 3.12+ recommended (developed/verified on CPython 3.14).
numpy==2.4.4
pandas==3.0.3
scipy==1.17.1
statsmodels==0.14.6
matplotlib==3.10.9
krippendorff==0.8.2
# transitive pins that statsmodels/pandas rely on (kept explicit for determinism)
patsy==1.0.2
python-dateutil==2.9.0.post0
